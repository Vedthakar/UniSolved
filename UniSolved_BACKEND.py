"""Legacy entrypoint retained only as a note for the old voice prototype."""


def main() -> None:
    print(
        "The old voice/OpenAI prototype has been retired from the active product.\n"
        "Run `python -m streamlit run UniSolved_WEBSITE.py` for the current UniSolved demo.\n"
        "Rotate any previously checked-in API keys before sharing this repository."
    )


if __name__ == "__main__":
    main()
